(()=>{'use strict';
function tag(node,{zoneId,chunkKey,kind='PROP',cullRadius=120,walkable=false,blocksSpawn=false}={}){node.metadata=Object.assign(node.metadata||{},{worldStatic:true,streamingStatic:true,zoneId,chunkKey,kind,cullRadius,walkable,blocksSpawn});return node}
function makeMat(scene,name,color,alpha=1){let m=scene.getMaterialByName?.(name);if(m)return m;m=new BABYLON.StandardMaterial(name,scene);m.diffuseColor=BABYLON.Color3.FromHexString(color);m.specularColor=new BABYLON.Color3(.02,.02,.02);m.alpha=alpha;return m}
class EnhancedWorldZoneAssetBuilder{constructor(scene){this.scene=scene;this.byZone=new Map()}mats(zone){if(this.byZone.has(zone.id))return this.byZone.get(zone.id);const v=zone.visual||{},id=zone.id,s=this.scene,grass=makeMat(s,`GAME2_Grass_${id}`,v.grass||'#55bd45'),road=makeMat(s,`GAME2_Road_${id}`,v.road||'#aa9270'),rock=makeMat(s,`GAME2_Rock_${id}`,v.rock||'#82958b'),water=makeMat(s,`GAME2_Water_${id}`,v.water||'#3ea9c7',.72),accent=makeMat(s,`GAME2_Accent_${id}`,v.accent||'#e7c060');const tex=window.GRASS_LUSH_TEXTURE||(window.GROUND_TEXTURE_ACTIVE&&window.GROUND_TEXTURE_ACTIVE.data);if(tex&&!v.cave){try{const t=new BABYLON.Texture(tex,s,false,true);t.uScale=14;t.vScale=14;grass.diffuseTexture=t}catch(_){}}const m={grass,road,rock,water,accent};this.byZone.set(id,m);return m}buildChunk({zone,chunkKey,cx,cz,size,signal}={}){if(signal?.aborted)return null;const s=this.scene,m=this.mats(zone),root=new BABYLON.TransformNode(`WorldChunk_${zone.id}_${chunkKey}`,s);tag(root,{zoneId:zone.id,chunkKey,kind:'CHUNK_ROOT',cullRadius:size*2});const ground=BABYLON.MeshBuilder.CreateGround(`Ground_${zone.id}_${chunkKey}`,{width:size,height:size,subdivisions:1},s);ground.position.set(cx,0,cz);ground.parent=root;ground.material=m.grass;ground.receiveShadows=true;ground.isPickable=true;tag(ground,{zoneId:zone.id,chunkKey,kind:'TERRAIN',cullRadius:size*2.5,walkable:true});for(const r of zone.terrain?.roads||[]){const mx=(r.a.x+r.b.x)/2,mz=(r.a.z+r.b.z)/2;if(Math.abs(mx-cx)>size*.85||Math.abs(mz-cz)>size*.85)continue;const len=Math.hypot(r.b.x-r.a.x,r.b.z-r.a.z),mesh=BABYLON.MeshBuilder.CreateBox(`Road_${zone.id}_${chunkKey}_${r.id}`,{width:r.width,height:.035,depth:len},s);mesh.position.set(mx,.025,mz);mesh.rotation.y=Math.atan2(r.b.x-r.a.x,r.b.z-r.a.z);mesh.parent=root;mesh.material=m.road;mesh.isPickable=false;tag(mesh,{zoneId:zone.id,chunkKey,kind:'ROAD',cullRadius:size*2})}for(const w of zone.terrain?.water||[]){if(Math.abs(w.center.x-cx)>size*.85||Math.abs(w.center.z-cz)>size*.85)continue;const mesh=BABYLON.MeshBuilder.CreateDisc(`Water_${zone.id}_${chunkKey}_${w.id}`,{radius:w.radius,tessellation:28},s);mesh.position.set(w.center.x,.04,w.center.z);mesh.rotation.x=Math.PI/2;mesh.parent=root;mesh.material=m.water;mesh.isPickable=false;tag(mesh,{zoneId:zone.id,chunkKey,kind:'WATER',cullRadius:size*2})}for(const p of zone.pois||[]){if(Math.abs(p.position.x-cx)>size*.58||Math.abs(p.position.z-cz)>size*.58||p.type==='WAYPOINT')continue;let mesh;if(zone.visual?.cave)mesh=BABYLON.MeshBuilder.CreatePolyhedron(`POI_${p.id}`,{type:1,size:1.7},s);else if(/altar|shrine|market|cultivation/i.test(p.id))mesh=BABYLON.MeshBuilder.CreateCylinder(`POI_${p.id}`,{height:3.6,diameterBottom:2.1,diameterTop:.45,tessellation:6},s);else mesh=BABYLON.MeshBuilder.CreateCylinder(`POI_${p.id}`,{height:4.2,diameterBottom:2.4,diameterTop:.35,tessellation:7},s);mesh.position.set(p.position.x,zone.visual?.cave?1.3:2,p.position.z);mesh.parent=root;mesh.material=/lake|spring/i.test(p.id)?m.water:m.accent;mesh.isPickable=false;tag(mesh,{zoneId:zone.id,chunkKey,kind:'LANDMARK',cullRadius:130,blocksSpawn:true})}if(zone.visual?.cave){const roof=BABYLON.MeshBuilder.CreateBox(`CaveRoof_${zone.id}_${chunkKey}`,{width:size,height:2,depth:size},s);roof.position.set(cx,8,cz);roof.parent=root;roof.material=m.rock;roof.isPickable=false;tag(roof,{zoneId:zone.id,chunkKey,kind:'CAVE_ROOF',cullRadius:size*2})}return{root,dispose:()=>root.dispose(false,false)}}dispose(){for(const m of this.byZone.values())for(const x of Object.values(m))x.dispose?.();this.byZone.clear()}}
window.EnhancedWorldZoneAssetBuilder=EnhancedWorldZoneAssetBuilder;
})();