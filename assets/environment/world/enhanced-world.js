(()=>{'use strict';
function tag(node,{zoneId,chunkKey,kind='PROP',cullRadius=120,walkable=false,blocksSpawn=false}={}){node.metadata=Object.assign(node.metadata||{},{worldStatic:true,streamingStatic:true,zoneId,chunkKey,kind,cullRadius,walkable,blocksSpawn});return node}
function makeMat(scene,name,color){let m=scene.getMaterialByName?.(name);if(m)return m;m=new BABYLON.StandardMaterial(name,scene);m.diffuseColor=BABYLON.Color3.FromHexString(color);m.specularColor=new BABYLON.Color3(.02,.02,.02);return m}
class EnhancedWorldZoneAssetBuilder{
 constructor(scene){this.scene=scene;this.materials=null}
 ensureMaterials(){if(this.materials)return this.materials;const s=this.scene,grass=makeMat(s,'GAME2_Grass','#4dbb3e'),road=makeMat(s,'GAME2_Road','#a18b6d'),rock=makeMat(s,'GAME2_Rock','#82958b'),water=makeMat(s,'GAME2_Water','#3ea9c7'),shrine=makeMat(s,'GAME2_Shrine','#c94237');water.alpha=.72;const tex=window.GRASS_LUSH_TEXTURE||(window.GROUND_TEXTURE_ACTIVE&&window.GROUND_TEXTURE_ACTIVE.data);if(tex){try{const t=new BABYLON.Texture(tex,s,false,true);t.uScale=18;t.vScale=18;grass.diffuseTexture=t}catch(_){}}return this.materials={grass,road,rock,water,shrine}}
 buildChunk({zone,chunkKey,cx,cz,size,signal}={}){if(signal?.aborted)return null;const s=this.scene,m=this.ensureMaterials(),root=new BABYLON.TransformNode(`WorldChunk_${zone.id}_${chunkKey}`,s);tag(root,{zoneId:zone.id,chunkKey,kind:'CHUNK_ROOT',cullRadius:size*2});
 const ground=BABYLON.MeshBuilder.CreateGround(`Ground_${zone.id}_${chunkKey}`,{width:size,height:size,subdivisions:1},s);ground.position.set(cx,0,cz);ground.parent=root;ground.material=m.grass;ground.receiveShadows=true;ground.isPickable=true;tag(ground,{zoneId:zone.id,chunkKey,kind:'TERRAIN',cullRadius:size*2.5,walkable:true});
 for(const r of zone.terrain?.roads||[]){const midX=(r.a.x+r.b.x)/2,midZ=(r.a.z+r.b.z)/2;if(Math.abs(midX-cx)>size*.8||Math.abs(midZ-cz)>size*.8)continue;const len=Math.hypot(r.b.x-r.a.x,r.b.z-r.a.z),mesh=BABYLON.MeshBuilder.CreateBox(`Road_${chunkKey}_${r.id}`,{width:r.width,height:.03,depth:len},s);mesh.position.set(midX,.02,midZ);mesh.rotation.y=Math.atan2(r.b.x-r.a.x,r.b.z-r.a.z);mesh.parent=root;mesh.material=m.road;mesh.isPickable=false;tag(mesh,{zoneId:zone.id,chunkKey,kind:'ROAD',cullRadius:size*2})}
 for(const w of zone.terrain?.water||[]){if(Math.abs(w.center.x-cx)>size*.8||Math.abs(w.center.z-cz)>size*.8)continue;const mesh=BABYLON.MeshBuilder.CreateDisc(`Water_${chunkKey}_${w.id}`,{radius:w.radius,tessellation:32},s);mesh.position.set(w.center.x,.035,w.center.z);mesh.rotation.x=Math.PI/2;mesh.parent=root;mesh.material=m.water;mesh.isPickable=false;tag(mesh,{zoneId:zone.id,chunkKey,kind:'WATER',cullRadius:size*2})}
 const pois=(zone.pois||[]).filter(p=>Math.abs(p.position.x-cx)<=size*.55&&Math.abs(p.position.z-cz)<=size*.55);for(const p of pois)if(p.type==='LANDMARK'){const h=BABYLON.MeshBuilder.CreateCylinder(`POI_${p.id}`,{height:4,diameterBottom:2.2,diameterTop:.3,tessellation:6},s);h.position.set(p.position.x,2,p.position.z);h.parent=root;h.material=p.id.includes('bamboo')?m.shrine:m.rock;h.isPickable=false;tag(h,{zoneId:zone.id,chunkKey,kind:'LANDMARK',cullRadius:150,blocksSpawn:true})}
 return{root,dispose:()=>root.dispose(false,true)}}
}
window.EnhancedWorldZoneAssetBuilder=EnhancedWorldZoneAssetBuilder;
})();