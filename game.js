const canvas=document.getElementById('renderCanvas'),isTouchDevice=matchMedia('(pointer: coarse)').matches||'ontouchstart'in window,isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1),deviceDpr=Math.max(1,Math.min(window.devicePixelRatio||1,isTouchDevice?2:2.5));if(typeof BABYLON!=='undefined'&&BABYLON.Texture)BABYLON.Texture.DEFAULT_ANISOTROPIC_FILTERING_LEVEL=isTouchDevice?2:4;const engine=new BABYLON.Engine(canvas,false,{preserveDrawingBuffer:false,stencil:false,adaptToDeviceRatio:false,antialias:false,powerPreference:'high-performance',audioEngine:false});engine.setHardwareScalingLevel(isTouchDevice?Math.max(1,(window.devicePixelRatio||1)/deviceDpr):1);engine.disablePerformanceMonitorInBackground=true;const scene=new BABYLON.Scene(engine);scene.clearColor=new BABYLON.Color4(.58,.84,.97,1);scene.ambientColor=new BABYLON.Color3(.38,.43,.42);scene.fogMode=BABYLON.Scene.FOGMODE_NONE;scene.skipPointerMovePicking=true;scene.autoClearDepthAndStencil=false;if(scene.imageProcessingConfiguration){const ip=scene.imageProcessingConfiguration;ip.toneMappingEnabled=true;ip.toneMappingType=BABYLON.ImageProcessingConfiguration.TONEMAPPING_ACES;ip.exposure=isTouchDevice?1.38:1.34;ip.contrast=1.04;ip.vignetteEnabled=false;ip.colorCurvesEnabled=true;const c=new BABYLON.ColorCurves();c.globalSaturation=18;c.globalExposure=5;c.highlightsExposure=3;c.shadowsExposure=7;c.midtonesSaturation=10;ip.colorCurves=c}const hemi=new BABYLON.HemisphericLight('sky',new BABYLON.Vector3(0,1,0),scene);hemi.intensity=1.55;hemi.diffuse=new BABYLON.Color3(1,1,.98);hemi.specular=new BABYLON.Color3(.38,.43,.44);hemi.groundColor=new BABYLON.Color3(.58,.74,.48);const sun=new BABYLON.DirectionalLight('sun',new BABYLON.Vector3(-.42,-1,.32),scene);sun.position.set(120,200,-120);sun.intensity=1.46;sun.diffuse=new BABYLON.Color3(1,.97,.84);sun.specular=new BABYLON.Color3(.64,.72,.76);const playerFill=new BABYLON.HemisphericLight('playerFill',new BABYLON.Vector3(0,.35,-1),scene);playerFill.intensity=.46;playerFill.diffuse=new BABYLON.Color3(.92,.98,1);playerFill.groundColor=new BABYLON.Color3(.38,.44,.38);const shadow=new BABYLON.ShadowGenerator(isTouchDevice?512:1024,sun);shadow.useBlurExponentialShadowMap=true;shadow.blurKernel=isTouchDevice?4:10;shadow.darkness=.14;shadow.bias=.002;shadow.normalBias=.01;const camera=new BABYLON.ArcRotateCamera('camera',Math.PI/2,1.02,23.5,new BABYLON.Vector3(0,.6,0),scene);camera.inputs.clear();camera.panningSensibility=0;camera.lowerBetaLimit=.9;camera.upperBetaLimit=1.2;camera.lowerRadiusLimit=6;camera.upperRadiusLimit=220;camera.fov=.5;camera.minZ=.1;camera.maxZ=isTouchDevice?1200:3000;let pipeline=null;try{pipeline=new BABYLON.DefaultRenderingPipeline('MobilePostProcess',false,scene,[camera]);pipeline.fxaaEnabled=true;pipeline.bloomEnabled=!isTouchDevice;pipeline.bloomThreshold=.92;pipeline.bloomWeight=.10;pipeline.bloomKernel=24;pipeline.bloomScale=.5;pipeline.imageProcessingEnabled=true;pipeline.samples=1}catch(e){console.warn('[GameRuntime] pipeline fallback',e)}const player=new BABYLON.TransformNode('PlayerRoot',scene);player.position.set(0,0,0);player.rotation.y=0;camera.lockedTarget=player;engine.runRenderLoop(()=>{if(!document.hidden)scene.render()});const appContainer=document.getElementById('gameApp');let resizeRaf=0;const scheduleResize=()=>{if(resizeRaf)return;resizeRaf=requestAnimationFrame(()=>{resizeRaf=0;engine.resize()})};if(window.ResizeObserver&&appContainer)new ResizeObserver(scheduleResize).observe(appContainer);window.addEventListener('resize',scheduleResize,{passive:true});window.addEventListener('orientationchange',scheduleResize,{passive:true});window.GameRuntime={engine,scene,player,camera,shadow,pipeline,playerFill,capabilities:{isTouchDevice,isIOS,deviceDpr}};