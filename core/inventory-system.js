(()=>{'use strict';
class InventorySystem{constructor({store=window.GameServices?.economyStore,items=window.GameServices?.items,transactions=window.GameServices?.transactions,events=window.GameServices?.events}={}){Object.assign(this,{store,items,transactions,events})}state(){return this.store.state.inventory}used(){return Object.keys(this.state().instances).length+Object.values(this.state().stacks).filter(x=>x>0).length}hasSpace(extra=1){return this.used()+extra<=this.state().capacity}listInstances(){return Object.values(this.state().instances)}listStacks(){return Object.entries(this.state().stacks).map(([baseId,quantity])=>({baseId,quantity}))}addStack(baseId,qty,{idempotencyKey=null}={}){qty=Math.floor(Number(qty)||0);const base=this.items.base(baseId);if(!base?.stackable||qty<=0)return{ok:false,reason:'INVALID_STACK'};return this.transactions.run({idempotencyKey:idempotencyKey||`inv-stack:${baseId}:${Date.now()}:${qty}`,resources:[`inventory:stack:${baseId}`],validate:()=>({ok:this.state().stacks[baseId]>0||this.hasSpace(1),reason:'INVENTORY_FULL'}),apply:()=>{this.state().stacks[baseId]=(Number(this.state().stacks[baseId])||0)+qty;this.store.save();this.events?.emit?.('inventory:changed',{kind:'STACK_ADD',baseId,qty});return true}})}addInstance(item,{idempotencyKey=null,toRecoveryOnFull=true}={}){if(!item?.instanceId)return{ok:false,reason:'INVALID_ITEM'};return this.transactions.run({idempotencyKey:idempotencyKey||`inv-instance:${item.instanceId}`,resources:[`inventory:instance:${item.instanceId}`],validate:()=>({ok:!this.state().instances[item.instanceId],reason:'DUPLICATE_ITEM'}),apply:()=>{if(!this.hasSpace(1)){if(toRecoveryOnFull){this.state().recovery.push(item);this.store.save();this.events?.emit?.('inventory:recoveryQueued',{instanceId:item.instanceId});return{recovery:true}}throw Object.assign(new Error('INVENTORY_FULL'),{code:'INVENTORY_FULL'})}this.state().instances[item.instanceId]=item;this.store.save();this.events?.emit?.('inventory:changed',{kind:'INSTANCE_ADD',instanceId:item.instanceId});return{recovery:false}}})}removeStack(baseId,qty){qty=Math.floor(Number(qty)||0);const cur=Number(this.state().stacks[baseId])||0;if(qty<=0||cur<qty)return false;this.state().stacks[baseId]=cur-qty;if(this.state().stacks[baseId]<=0)delete this.state().stacks[baseId];this.store.save();return true}removeInstance(id){const x=this.state().instances[id];if(!x)return null;delete this.state().instances[id];this.store.save();return x}reserve(key,items=[]){if(!key||this.state().reservations[key])return false;this.state().reservations[key]=items;this.store.save();return true}release(key){if(!this.state().reservations[key])return false;delete this.state().reservations[key];this.store.save();return true}claimRecovery(){let n=0;while(this.state().recovery.length&&this.hasSpace(1)){const item=this.state().recovery.shift();this.state().instances[item.instanceId]=item;n++}if(n)this.store.save();return n}}
window.GameCore=window.GameCore||{};window.GameCore.InventorySystem=InventorySystem;window.GameCore.inventory=window.GameCore.inventory||new InventorySystem();window.GameServices=Object.assign(window.GameServices||{},{inventory:window.GameCore.inventory});
})();